{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build dev",
            "type": "shell",
            "command": "cargo build",
            "group": {
                "kind": "build",
                "isDefault": true
            },

        },
        {
            "label": "build release",
            "type": "shell",
            "command": "cargo build --release",
            "group": {
                "kind": "build",
                
            },

        }
    ]
}